Generic single-database configuration.

## Migration Workflow

1. `> flask db init --directory=migrations/test`
2. `> flask db migrate --directory=migrations/test`
3. `> flask db upgrade --directory=migrations/test`